import React from "react";
import classes from "./footer.module.scss";

const Footer = () => {
  return (
    <div className={classes.footer}>
      Royal Enfield | *T&C apply. Images shown here may differ from the actual
      product.
    </div>
  );
};

export default Footer;
